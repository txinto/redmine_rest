require 'spec_helper'

describe RedmineRest do
  it 'has a version number' do
    expect(RedmineRest::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
